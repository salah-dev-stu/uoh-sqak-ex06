# ── Environment / tooling noise (NEVER commit) ───────────────────────────
.blender-toolkit/
.playwright-mcp/
.claude/
.DS_Store

# ── Secrets ──────────────────────────────────────────────────────────────
.env
*.env
!.env-example
credentials.json
token.json
*.pem
*.key

# ── Submission forms — contain national ID numbers (PII); Moodle-only, never
#    in the public repo. Generated by scripts/fill_submission_pdf.py.
uoh-sqak-ex06.docx
uoh-sqak-ex06.pdf
uoh-rl07-ex06-template.docx

# ── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# ── Test / coverage / lint caches ────────────────────────────────────────
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ── Runtime artifacts (generated game logs kept selectively) ─────────────
*.log
reports/runs/
gate_ledger.json

# ── Editors ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp

# Live replay temp/artifacts (non-deterministic; regenerate with make replay-live)
.live-tmp/
reports/live_report.json
reports/transcripts/live/
