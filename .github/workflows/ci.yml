name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Pin Python 3.13 explicitly — never let CI drift to 3.14 (HW4 trap, R13).
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Sync (dev only — no heavy/real extras; everything external is mocked)
        run: uv sync --dev

      - name: Ruff (zero failures, R8)
        run: uv run ruff check .

      - name: File-size guard — ≤150 lines raw AND logical (R7)
        run: uv run python scripts/check_file_lines.py

      - name: Version single-source sync (R5)
        run: uv run python scripts/check_version_sync.py

      - name: Tests + coverage ≥85% (R6/R9, fully mocked — no keys/servers)
        run: uv run pytest
